# -*- coding: utf-8 -*-
"""newsbert

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/#fileId=https%3A//storage.googleapis.com/kaggle-colab-exported-notebooks/newsbert-a9a1ed5e-989c-4b3e-81f3-0a7b94ab9504.ipynb%3FX-Goog-Algorithm%3DGOOG4-RSA-SHA256%26X-Goog-Credential%3Dgcp-kaggle-com%2540kaggle-161607.iam.gserviceaccount.com/20240410/auto/storage/goog4_request%26X-Goog-Date%3D20240410T064351Z%26X-Goog-Expires%3D259200%26X-Goog-SignedHeaders%3Dhost%26X-Goog-Signature%
"""

!pip install transformers datasets torch

#Preprocess the Dataset
#We need to tokenize our text data (news articles) before it can be fed to BERT. We also need to adjust the labels since BERT expects them to start from 0.
from datasets import load_dataset
from transformers import BertTokenizerFast

# Load the AG News dataset
dataset = load_dataset('ag_news')

# Load the BERT tokenizer
tokenizer = BertTokenizerFast.from_pretrained('bert-base-uncased')

# Function to tokenize the text
def tokenize_function(examples):
    return tokenizer(examples['text'], padding='max_length', truncation=True, max_length=128)

# Tokenize the dataset
tokenized_datasets = dataset.map(tokenize_function, batched=True)

# Example of accessing a tokenized example
print(tokenized_datasets['train'][0])

# Split the data into training and validation sets
small_train_dataset = tokenized_datasets['train'].shuffle(seed=42).select(range(2000)) # smaller size for quicker training
small_eval_dataset = tokenized_datasets['test'].shuffle(seed=42).select(range(500))

#Load the BERT Model for Sequence Classification

from transformers import BertForSequenceClassification

model = BertForSequenceClassification.from_pretrained('bert-base-uncased', num_labels=4)

!pip install transformers[torch] accelerate -U

import os
os.environ["WANDB_DISABLED"] = "true"

#Define Training Arguments and Train the Model
from transformers import BertForSequenceClassification, Trainer, TrainingArguments
import numpy as np

training_args = TrainingArguments(
    output_dir='./results',           # Output directory for model checkpoints
    num_train_epochs=3,               # Number of training epochs
    per_device_train_batch_size=8,    # Batch size for training
    per_device_eval_batch_size=8,     # Batch size for evaluation
    warmup_steps=500,                 # Number of warmup steps
    weight_decay=0.01,                # Weight decay rate
    logging_dir='./logs',             # Directory for logging
    evaluation_strategy='epoch',      # Evaluation strategy
)

# Initialize the Trainer
trainer = Trainer(
    model=model,
    args=training_args,
    train_dataset=small_train_dataset,
    eval_dataset=small_eval_dataset,
    compute_metrics=lambda p: {"accuracy": (np.argmax(p.predictions, axis=1) == p.label_ids).mean()}
)

# Start fine-tuning
trainer.train()

eval_results = trainer.evaluate()
print(eval_results)

